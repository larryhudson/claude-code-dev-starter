# API URL (for production builds)
VITE_API_URL=http://localhost:8000

# Allowed hosts for dev server (comma-separated for multiple)
# Example: VITE_ALLOWED_HOSTS=myhost.example.com,another.example.com
VITE_ALLOWED_HOSTS=
