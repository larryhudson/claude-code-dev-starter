---
name: CI

"on":
  push:
  pull_request:

jobs:
  python-lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: 'pyproject.toml'

      - name: Install dependencies
        run: uv sync

      - name: Lint with ruff
        run: uv run ruff check .
        continue-on-error: true

      - name: Format check with ruff
        run: uv run ruff format --check .
        continue-on-error: true

      - name: Type check with ty
        run: uv run ty check .
        continue-on-error: true

  # Agent evaluations - runs after linting passes
  eval:
    runs-on: ubuntu-latest
    needs: [python-lint]
    # Only run if ANTHROPIC_API_KEY secret is configured
    if: ${{ vars.RUN_EVALS == 'true' }}

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: 'pyproject.toml'

      - name: Install dependencies
        run: uv sync --all-groups

      - name: Run evaluations
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: uv run pytest evals/ -v --tb=short

    # Uncomment to use pre-commit for additional checks
  # pre-commit:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     - uses: pre-commit/action@v3.0.0  # yamllint disable
